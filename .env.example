# Copy to .env and paste the pooled connection string from Neon.
DATABASE_URL=postgresql://USER:PASSWORD@ep-example-123456.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
PORT=3001
# Use the browser UI's deployed address in production. The default permits the local UI.
CLIENT_ORIGIN=http://localhost:5500
